apiVersion: v1
kind: Service
metadata:
  name: redpanda-console
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
  selector:
    app: redpanda-console
