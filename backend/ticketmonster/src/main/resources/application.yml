spring:
  application:
    name: ticketmonster

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:ticketmonster}
    username: ${POSTGRES_USER:ticketmonster}
    password: ${POSTGRES_PASSWORD:ticketmonster}
    hikari:
      maximum-pool-size: ${HIKARI_MAX_POOL_SIZE:20}
      minimum-idle: ${HIKARI_MIN_IDLE:5}
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  data:
    mongodb:
      uri: mongodb://${MONGO_USER:ticketmonster}:${MONGO_PASSWORD:ticketmonster}@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:ticketmonster_catalog}?authSource=${MONGO_AUTH_DB:admin}
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 2000ms

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
    consumer:
      group-id: ticketmonster
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: es.janrax.ticketmonster.*

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/ticket-monster}
          jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:http://localhost:8080/realms/ticket-monster/protocol/openid-connect/certs}

  graphql:
    graphiql:
      enabled: ${GRAPHIQL_ENABLED:true}
    schema:
      locations: classpath:graphql/**/
    path: /graphql

  threads:
    virtual:
      enabled: true

server:
  port: ${SERVER_PORT:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: ${spring.application.name}
    export:
      prometheus:
        enabled: true

resilience4j:
  circuitbreaker:
    instances:
      catalog:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 3
      reservation:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 3
      payment:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 3
  ratelimiter:
    instances:
      global:
        limit-for-period: 100
        limit-refresh-period: 1m
        timeout-duration: 0
  timelimiter:
    instances:
      default:
        timeout-duration: 5s

ticketmonster:
  reservation:
    ttl-minutes: ${RESERVATION_TTL_MINUTES:10}
    max-tickets-per-customer: ${MAX_TICKETS_PER_CUSTOMER:3}
  queue:
    batch-size: ${QUEUE_BATCH_SIZE:500}
    batch-interval-ms: ${QUEUE_BATCH_INTERVAL_MS:2000}
    token-ttl-minutes: ${QUEUE_TOKEN_TTL_MINUTES:5}
